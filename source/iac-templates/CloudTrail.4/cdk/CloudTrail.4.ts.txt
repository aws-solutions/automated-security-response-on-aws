// CloudTrail.4 — CloudTrail log file validation should be enabled
//
// ASR remediation enables log file validation on the CloudTrail trail.
// To apply this fix, add or update the enableFileValidation property on your Trail construct.

const trail = new cloudtrail.Trail(this, '{trailName}', {
  enableFileValidation: true,
});
