# SecretsManager.3 — Remove unused Secrets Manager secrets
#
# ASR remediation deletes secrets that have been unused for a specified number
# of days (default 90 days), with a 30-day recovery window.
# No IaC change is needed. Removing unused secrets is an operational activity
# that cannot be represented as an infrastructure-as-code resource property.
