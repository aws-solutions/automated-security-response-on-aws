# GuardDuty.2 — GuardDuty filter should be tagged
#
# ASR remediation tags the GuardDuty filter with the required tag keys
# specified in the Security Hub control parameters.
# To apply this fix, add the required tags to your GuardDuty filter resource.

resource "aws_guardduty_filter" "{filter}" {
  tags = {
    "SO0111-ASR-GuardDutyResource" = ""
  }
}
