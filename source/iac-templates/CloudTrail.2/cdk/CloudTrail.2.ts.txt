// CloudTrail.2 — CloudTrail should have encryption at rest enabled
//
// ASR remediation enables SSE-KMS encryption on the CloudTrail trail using a KMS CMK.
// To apply this fix, add or update the encryptionKey property on your Trail construct.
// NOTE: <KMS_KEY> — your KMS key for trail encryption.

const trail = new cloudtrail.Trail(this, '{trailName}', {
  encryptionKey: <KMS_KEY>,
});
