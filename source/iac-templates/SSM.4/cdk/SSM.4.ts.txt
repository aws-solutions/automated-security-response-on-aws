// SSM.4 — SSM documents should not be public
//
// ASR remediation removes public access from the SSM document by revoking
// the shared "all" account permission.
// No IaC change is needed. Documents created via CDK are private by default.
// If you explicitly shared a document (e.g. via a custom resource), remove
// that sharing configuration from your code.
