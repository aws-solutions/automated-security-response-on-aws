# SSM.4 — SSM documents should not be public
#
# ASR remediation removes public access from the SSM document by revoking
# the shared "all" account permission.
# No IaC change is needed. Documents created via CloudFormation are private
# by default. If you explicitly shared a document outside of CloudFormation,
# remove that sharing configuration.
